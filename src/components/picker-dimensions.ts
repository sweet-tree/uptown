/** Min height for combobox trigger row (touch + readability). */
export const COMBO_TRIGGER_MIN_PX = 40;

/** Max dropdown height: team rows are two lines (~52px each) → ≥5 visible. */
export const TEAM_LIST_MAX_PX = 280;

/** Roster rows are denser; still ≥5 lines with padding. */
export const ROSTER_LIST_MAX_PX = 280;
