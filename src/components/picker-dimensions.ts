/** Min height for combobox trigger row (touch target + comfortable type). */
export const COMBO_TRIGGER_MIN_PX = 46;

/** Max dropdown height: team rows are two lines (~52px each) → ≥5 visible. */
export const TEAM_LIST_MAX_PX = 272;

/** Roster rows are single-line dense; ≥5 rows with list padding. */
export const ROSTER_LIST_MAX_PX = 232;
